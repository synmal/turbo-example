<%= form_with url: todos_path, class: 'd-flex gap-2', model: Todo.new do |f| %>
  <%= f.text_field :description, class: 'form-control', placeholder: 'Todo Description' %>
  <%= f.submit 'Add Todo', class: 'btn btn-primary' %>
<% end %>
